const updateUserProfile = require("./update-user-profile");
const testing = require("./testing");
const { notFound } = require("@feathersjs/express");
// eslint-disable-next-line no-unused-vars
module.exports = function (app) {
  // const app = this;
  // console.log("heyyyy????");s
  // app.post("/signuptest", updateUserProfile(app));
  // app.use(notFound());
  // app.use(logger(app));
  // app.use(handler());
  // app.use('patch', updateUserProfile());
  // app.use('siffff', testing());
};
